{
  "name": "qiankun-stack",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run --parallel start",
    "clean": "lerna clean --yes"
  },
  "devDependencies": {
    "lerna": "^4.x",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "engines": { "node": ">=16.14" }
}
  